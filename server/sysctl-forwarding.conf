# IP forwarding configuration for the WireGuard server
# Location: /etc/sysctl.conf (append this line, or uncomment if present)
#
# Required so the VM can route traffic between the WireGuard tunnel
# and the local LAN, rather than just terminating the tunnel locally.
#
# Apply with: sudo sysctl -p

net.ipv4.ip_forward=1
